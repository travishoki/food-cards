export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use

echo "\n==================== Git Commit ===================="

echo "\n---------- Lint Staged ----------\n"

yarn lint-staged


